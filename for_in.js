const newObj = {
  myName: 'nilesh',
  rollNo: 5454,
  course: 'B.sc IT',
};

const arr = [1, 2, 5];

for (const key in arr) {
  console.log(`${key} : ${arr[key]} `);
}

/*
    for in  : for array and object
    for of :  for array, set, map, strings
    */
